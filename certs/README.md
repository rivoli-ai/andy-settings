# Certificates

Place corporate CA certificates (`.crt` or `.pem` files) in this directory.
They will be automatically trusted during Docker builds.

Files in this directory are ignored by git (except this README).
